const user = (state = {}, action: {type: string}) => {
  switch (action.type) {
    case "login":
      
      return 
    case "signup":
      return 
    case "logout":
      return 
    default:
      break;
  }
}

export default user;